
#include <iostream>
#include "constantes.h"

//classe utilisée surtout au début du projet, avant les constructeurs simples de Vecteur pour un vecteur nul simplement créé, ou avant la cération de la classe Gravite pour g 

using namespace std;

extern const Vecteur g(0.0,-9.81,0);
extern const double pi(3.1415926535);
extern const double rho_air(1.2);
extern const Vecteur vecteur_nul(3);
